{% extends 'base.html.twig'%}
{% block tilte %}
    {{parent()}}
    page 2 
    {% endblock %}
    {% block body %}
        
        Ajout des utilisateurs 
        {{form_start (formulaire,{'attr':{'action':path('valid')}})}}
        <input type="submit" class="btn btn-primary">Ajout</input>
        {{form_end(formulaire)}}
        
        {% endblock %}